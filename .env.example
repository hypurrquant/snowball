# Deployer key (for contracts + admin operations)
DEPLOYER_PRIVATE_KEY=0x_your_private_key_here
ADMIN_PRIVATE_KEY=0x_your_private_key_here

# Consumer Agent API
PORT=3000
CDP_PROVIDER_URL=http://localhost:3001

# CDP Provider Agent
CDP_PROVIDER_PORT=3001

# Chatbot
CHATBOT_PORT=3002
CONSUMER_API_URL=http://localhost:3000
OPENAI_API_KEY=sk-your-openai-api-key  # Optional — chatbot works without it (rule-based fallback)

# Deployed addresses (auto-detected from deployment output, or set manually)
# DEPLOYED_ADDRESSES_PATH=./packages/contracts-liquity/deployments/addresses-102031.json
