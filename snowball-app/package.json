{
  "name": "snowball-app",
  "version": "0.1.0",
  "private": true,
  "description": "Snowball — CDP + Lending FE + Agent Backend",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "dev:app": "pnpm --filter @snowball/app dev",
    "dev:consumer": "pnpm --filter @snowball/agent-consumer dev",
    "dev:provider": "pnpm --filter @snowball/agent-provider dev",
    "dev:chatbot": "pnpm --filter @snowball/agent-chatbot dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
