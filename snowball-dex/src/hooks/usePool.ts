// TODO: Implement pool data hook
// - Fetch pool list from SnowballFactory.poolByPair()
// - Read pool state via globalState() (price, tick, fee, pluginConfig)
// - Read liquidity via pool.liquidity()
// - Read dynamic fee via DynamicFeePlugin.getFee(pool)
// - Read user NFT positions via NonfungiblePositionManager
//   (balanceOf -> tokenOfOwnerByIndex -> positions)
// - Collect fees, decrease/remove liquidity actions

export {};
